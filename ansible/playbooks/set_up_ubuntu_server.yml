---
- name: Set up Ubuntu server
  hosts: ubuntu_server
  roles:
    - role: ubuntu_configure_netplan
      vars:
        netplan_type: server
    - ubuntu_set_up_nginx
    - role: ubuntu_set_up_tls_certificate
      vars:
        domain: some-domain.com
        email: admin@some-domain.com
